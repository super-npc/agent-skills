我有一個用 Remotion 寫的菱形展開轉場元件（檔案：DiamondRevealTransition.tsx），請幫我做以下調整：

1. 把欄數從 11 改成「___」
2. 把列數從 6 改成「___」
3. 把菱形大小從 180 改成「___」（單位 px）
4. 把每個菱形的延遲係數從 `(col + row) * 3` 改成「___」
5. 把菱形縮小的持續幀數從 25 改成「___」
6. 把菱形色塊的背景漸層從 `linear-gradient(135deg, #1e3a5f 0%, #0f172a 100%)` 改成「___」
7. 把 Scene B 的背景漸層從 `linear-gradient(135deg, #1a0533 0%, #0f0a1a 100%)` 改成「___」

請保留原本的菱形展開邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
