我有一個用 Remotion 寫的縮小放大轉場元件（檔案：ZoomOutInTransition.tsx），請幫我做以下調整：

1. 把場景 A 縮小的起始幀從 15 改成「___」
2. 把場景 A 縮小的結束幀從 50 改成「___」
3. 把場景 B 放大的起始幀從 50 改成「___」
4. 把場景 B 放大的結束幀從 80 改成「___」
5. 把中間的黑色背景從 `black` 改成「___」
6. 把 Scene A 的背景漸層從 `linear-gradient(135deg, #1e3a5f 0%, #0f172a 100%)` 改成「___」
7. 把 Scene B 的背景漸層從 `linear-gradient(135deg, #1a0533 0%, #0f0a1a 100%)` 改成「___」

請保留原本的縮小放大邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
