我有一個用 Remotion 寫的對開門轉場元件（檔案：SplitDoorsTransition.tsx），請幫我做以下調整：

1. 把動畫起始延遲從 20 改成「___」（`frame - 20` 中的延遲量）
2. 把 spring 的 damping 從 18 改成「___」
3. 把 spring 的 stiffness 從 100 改成「___」
4. 把分割點從畫面正中央（960px）改成「___」px（例如 640 代表左側偏移分割）
5. 把左門的移動距離從 -960 改成「___」px
6. 把右門的移動距離從 960 改成「___」px

請保留原本的對開門邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
