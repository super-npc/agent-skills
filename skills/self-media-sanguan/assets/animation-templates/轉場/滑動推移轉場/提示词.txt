我有一個用 Remotion 寫的滑動推移轉場元件（檔案：SlidePushTransition.tsx），請幫我做以下調整：

1. 把動畫的起始幀從 20 改成「___」（`frame - 20` 中的延遲量）
2. 把 spring 的 damping 從 20 改成「___」；想更有彈性請改小（例如 10），想更平穩請改大（例如 30）
3. 把 spring 的 stiffness 從 120 改成「___」；想更快請改大（例如 180），想更慢請改小（例如 80）
4. 把推移方向改為從左到右（目前是從右到左），請將 `aTranslateX` 與 `bTranslateX` 的正負號對調
5. 把 Scene A 的背景漸層改成「___」
6. 把 Scene B 的背景漸層改成「___」

請保留原本的滑動推移邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
