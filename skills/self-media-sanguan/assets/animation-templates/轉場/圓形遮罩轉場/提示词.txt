我有一個用 Remotion 寫的圓形遮罩轉場元件（檔案：MorphCircleTransition.tsx），請幫我做以下調整：

1. 把動畫起始延遲從 20 改成「___」（`frame - 20` 中的延遲量）
2. 把 spring 的 damping 從 20 改成「___」
3. 把 spring 的 stiffness 從 80 改成「___」；想更快請改大（例如 120），想更慢請改小（例如 50）
4. 把圓形擴張的最終半徑從 1200 改成「___」px
5. 把圓形展開的中心點從畫面中心 `960px 540px` 改成「___ ___」
6. 把 Scene A 的背景改成「___」、Scene B 的背景改成「___」

請保留原本的圓形 clipPath 遮罩邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
