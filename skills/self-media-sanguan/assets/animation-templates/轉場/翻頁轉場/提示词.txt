我有一個用 Remotion 寫的翻頁轉場元件（檔案：PageFlipTransition.tsx），請幫我做以下調整：

1. 把動畫起始延遲從 20 改成「___」（`frame - 20` 中的延遲量）
2. 把 spring 的 damping 從 22 改成「___」
3. 把 spring 的 stiffness 從 90 改成「___」
4. 把 perspective 景深從 1200px 改成「___」px（值越小 3D 效果越誇張）
5. 把翻轉軸從水平中心改為左側邊緣（`transformOrigin` 從 `"center center"` 改成 `"left center"`）
6. 把 Scene A 的背景改成「___」、Scene B 的背景改成「___」

請保留原本的 3D rotateY 翻頁邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
