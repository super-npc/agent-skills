我有一個用 Remotion 寫的交叉淡化轉場元件（檔案：FadeCrossTransition.tsx），請幫我做以下調整：

1. 把淡化的起始幀從 30 改成「___」
2. 把淡化的結束幀從 60 改成「___」
3. 把 Scene A 的背景漸層從 `linear-gradient(135deg, #1e3a5f 0%, #0f172a 100%)` 改成「___」
4. 把 Scene B 的背景漸層從 `linear-gradient(135deg, #1a0533 0%, #0f0a1a 100%)` 改成「___」
5. 把 Scene A 文字的陰影顏色從 `rgba(59,130,246,0.5)` 改成「___」
6. 把 Scene B 文字的陰影顏色從 `rgba(168,85,247,0.5)` 改成「___」

請保留原本的交叉淡化邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
