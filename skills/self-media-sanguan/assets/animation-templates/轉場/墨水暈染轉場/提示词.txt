我有一個用 Remotion 寫的墨水暈染轉場元件（檔案：InkSpreadTransition.tsx），請幫我做以下調整：

1. 把墨水擴散的起始幀從 15 改成「___」
2. 把墨水擴散的峰值幀從 50 改成「___」
3. 把墨水完全消退的結束幀從 75 改成「___」
4. 把墨水最大縮放倍率（峰值）從 3 改成「___」
5. 把墨水顏色從 `#000000` 改成「___」（例如 `#1a1a2e` 以配合深色主題）
6. 把墨水的模糊半徑從 30px 改成「___」px（值越大邊緣越柔和）
7. 把 Scene B 淡入的起始幀從 50 改成「___」、結束幀從 75 改成「___」

請保留原本的圓形縮放暈染邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
