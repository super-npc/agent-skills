我有一個用 Remotion 寫的像素化轉場元件（檔案：PixelateTransition.tsx），請幫我做以下調整：

1. 把動畫起始幀從 20 改成「___」
2. 把動畫結束幀從 60 改成「___」
3. 把模糊峰值發生的中間幀從 40 改成「___」
4. 把最大模糊半徑從 24 改成「___」px（值越大模糊越強烈）
5. 把 Scene A 的背景改成「___」
6. 把 Scene B 的背景改成「___」

請保留原本的模糊淡化邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
