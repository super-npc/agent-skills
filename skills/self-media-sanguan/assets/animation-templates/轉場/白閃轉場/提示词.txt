我有一個用 Remotion 寫的白閃轉場元件（檔案：FlashWhiteTransition.tsx），請幫我做以下調整：

1. 把閃光淡入的起始幀從 30 改成「___」
2. 把閃光最亮的幀從 45 改成「___」
3. 把閃光淡出的結束幀從 60 改成「___」
4. 把 Scene A 的背景漸層從 `linear-gradient(135deg, #1e3a5f 0%, #0f172a 100%)` 改成「___」
5. 把 Scene B 的背景漸層從 `linear-gradient(135deg, #1a0533 0%, #0f0a1a 100%)` 改成「___」
6. 把白色 overlay 改成其他顏色「___」

請保留原本的白閃邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
