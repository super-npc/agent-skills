我有一個用 Remotion 寫的旋轉縮放轉場元件（檔案：SpinZoomTransition.tsx），請幫我做以下調整：

1. 把轉場進度的起始幀從 15 改成「___」
2. 把轉場進度的結束幀從 75 改成「___」
3. 把場景 A 的旋轉角度從 180deg 改成「___」
4. 把場景 B 的旋轉角度從 -180deg 改成「___」
5. 把 Scene A 的背景漸層從 `linear-gradient(135deg, #1e3a5f 0%, #0f172a 100%)` 改成「___」
6. 把 Scene B 的背景漸層從 `linear-gradient(135deg, #1a0533 0%, #0f0a1a 100%)` 改成「___」

請保留原本的旋轉縮放邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
