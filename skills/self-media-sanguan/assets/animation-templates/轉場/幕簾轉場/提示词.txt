我有一個用 Remotion 寫的幕簾轉場元件（檔案：CurtainTransition.tsx），請幫我做以下調整：

1. 把左右門開始移動的幀從 20 改成「___」
2. 把左右門移動完成的幀從 70 改成「___」
3. 把左門移動距離從 -960 改成「___」（單位 px）
4. 把右門移動距離從 960 改成「___」（單位 px）
5. 把 Scene A 的背景漸層從 `linear-gradient(135deg, #1e3a5f 0%, #0f172a 100%)` 改成「___」
6. 把 Scene B 的背景漸層從 `linear-gradient(135deg, #1a0533 0%, #0f0a1a 100%)` 改成「___」

請保留原本的幕簾邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
