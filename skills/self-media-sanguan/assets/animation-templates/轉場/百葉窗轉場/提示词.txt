我有一個用 Remotion 寫的百葉窗轉場元件（檔案：BlindsTransition.tsx），請幫我做以下調整：

1. 把百葉窗條紋數量從 8 改成「___」
2. 把每條條紋的收縮起始幀偏移從 `i * 8` 改成「___」
3. 把條紋收縮的開始幀（相對偏移後）從 15 改成「___」
4. 把條紋收縮的結束幀（相對偏移後）從 45 改成「___」
5. 把 Scene A（條紋）的背景漸層從 `linear-gradient(135deg, #1e3a5f 0%, #0f172a 100%)` 改成「___」
6. 把 Scene B 的背景漸層從 `linear-gradient(135deg, #1a0533 0%, #0f0a1a 100%)` 改成「___」

請保留原本的百葉窗邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
