我有一個用 Remotion 寫的時鐘擦除轉場元件（檔案：WipeClockTransition.tsx），請幫我做以下調整：

1. 把動畫起始幀從 15 改成「___」
2. 把動畫結束幀從 75 改成「___」
3. 把擦除方向從「由左向右」改為「由右向左」（調整 `clipPath` 的 `inset` 方向）
4. 把 Scene A 的背景漸層改成「___」
5. 把 Scene B 的背景漸層改成「___」

請保留原本的擦除遮罩邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
