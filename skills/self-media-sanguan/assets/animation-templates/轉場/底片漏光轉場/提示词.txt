我有一個用 Remotion 寫的底片漏光轉場元件（檔案：FilmBurnTransition.tsx），請幫我做以下調整：

1. 把場景切換點從 frame 45 改成「___」
2. 把漏光淡入的起始幀從 30 改成「___」
3. 把漏光最強的幀從 45 改成「___」
4. 把漏光淡出的結束幀從 60 改成「___」
5. 把漏光層的放射漸層顏色從 `#ff6b00`、`#ff2200` 改成「___」
6. 把橘色 overlay 的背景色從 `rgba(255,120,0,0.3)` 改成「___」
7. 把 Scene A 的背景漸層從 `linear-gradient(135deg, #1e3a5f 0%, #0f172a 100%)` 改成「___」
8. 把 Scene B 的背景漸層從 `linear-gradient(135deg, #1a0533 0%, #0f0a1a 100%)` 改成「___」

請保留原本的底片漏光邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
