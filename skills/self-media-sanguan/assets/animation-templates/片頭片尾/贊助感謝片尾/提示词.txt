我有一個用 Remotion 寫的贊助感謝片尾元件（檔案：SponsorOutro.tsx），請幫我做以下調整：

1. 把品牌名稱從 `BRAND NAME` 改成「___」
2. 把品牌 Slogan 從 `Empowering creators worldwide` 改成「___」
3. 把優惠碼文字從「輸入優惠碼 REMOTION 享 85 折優惠」改成「___」
4. 把「了解更多 →」按鈕文字改成「___」
5. 把背景漸層從 `#0f172a → #0c1445` 改成「___」
6. 把優惠碼文字顏色從 `#fbbf24` 改成「___」

請保留原本的淡入與縮放動畫邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
