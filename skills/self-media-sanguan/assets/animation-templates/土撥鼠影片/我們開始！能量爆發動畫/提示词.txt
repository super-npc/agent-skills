我有一個用 Remotion 寫的場景元件（檔案：Scene27-LetsStart.tsx），請幫我做以下調整：

1. 修改主文字（目前是 "我們開始"）與字體大小（目前是 88px）、字距（目前是 12）。
2. 更換播放圖示（`PlayIcon`）的顏色（目前是 `#00D4AA` 綠色）。
3. 調整能量射線的顏色（目前是綠色/藍色/白色交替）與數量（目前 12 道）。
4. 修改星點（`Sparkle`）的顏色與數量（`revealSparkles` 陣列有 6 個）。

請保留原本的脈衝環動畫與彈跳入場效果，只修改我指定的部分，然後把完整的修改後程式碼給我。
