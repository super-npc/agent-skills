我有一個用 Remotion 寫的場景元件（檔案：Scene09-McpChapterCard.tsx），請幫我做以下調整：

1. 修改左側標籤（目前是 "PHASE 3"）與主標題（目前是 "MCP"）的文字。
2. 更換右側兩個平台圖示（目前是 GitHub 與 Vercel），改成你想要的服務圖示。
3. 調整 accent 色（目前是 `#00D4AA`）與 accentSecondary（目前是 `#4DA3FF`）。
4. 修改動畫持續時長（目前 75 幀 / 2.5 秒）。

請保留原本的插頭描邊動畫與連接線效果，只修改我指定的部分，然後把完整的修改後程式碼給我。
