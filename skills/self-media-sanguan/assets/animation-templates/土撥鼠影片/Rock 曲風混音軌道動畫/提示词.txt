我有一個用 Remotion 寫的場景元件（檔案：Scene15-RockRemix.tsx），請幫我做以下調整：

1. 修改 `TRACKS` 陣列中四條軌道的 `label`（目前是 Piano/Guitar/Drums/Bass）與出現時間點（`appearFrame`）。
2. 修改頂部徽章文字（目前是 "ROCK"）與顏色。
3. 調整速度計最終顯示值（目前是 "1.5x"）與指針目標角度。
4. 更換四種樂器的顏色配置（目前是白/紅/黃/藍）。

請保留原本的波形動畫與速度計效果，只修改我指定的部分，然後把完整的修改後程式碼給我。
