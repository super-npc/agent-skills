我有一個用 Remotion 寫的場景元件（檔案：Scene07-SoftwareCriteria.tsx），請幫我做以下調整：

1. 修改 `cards` 陣列中三張卡片的 `text` 文字內容，換成你自己的評選條件。
2. 更換三個 SVG 圖示（`ImportEditIcon`、`MCPConnectorIcon`、`OpenSourceIcon`），可改用更適合主題的形狀。
3. 調整標題文字（目前是 "軟體選擇的三個條件"）。
4. 修改卡片寬度（目前是 1400px）與卡片間距（目前是 32px）以適應不同螢幕比例。

請保留原本的從左側滑入動畫與徽章脈衝效果，只修改我指定的部分，然後把完整的修改後程式碼給我。
