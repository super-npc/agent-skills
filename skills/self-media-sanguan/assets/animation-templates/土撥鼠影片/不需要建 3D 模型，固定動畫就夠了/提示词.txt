我有一個用 Remotion 寫的場景元件（檔案：Scene65-No3DNeeded.tsx），請幫我做以下調整：

1. 修改禁止標籤文字（目前是「不需要建 3D 模型」）
2. 更改固定動畫徽章文字（目前是「固定動畫就夠了」）
3. 調整 3D 立方體的顏色（目前使用 accentSecondary #4DA3FF）
4. 修改播放器標題（目前是「Animation.mp4」）

請保留原本的動畫效果，只修改我指定的部分，然後把完整的修改後程式碼給我。
