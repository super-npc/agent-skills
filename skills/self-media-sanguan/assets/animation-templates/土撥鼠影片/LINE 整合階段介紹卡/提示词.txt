我有一個用 Remotion 寫的場景元件（檔案：Scene07-LinePhaseIntro.tsx），請幫我做以下調整：

1. 修改 `CHECKLIST-ITEMS` 陣列中的標籤文字，換成你自己的整合步驟清單。
2. 調整左側標題（目前是 "LINE"）與副標題（目前是 "PHASE 2"）為你想要的名稱。
3. 更改 Logo 圖示（`LineLogoIcon`）的顏色或形狀，使其符合你的品牌。
4. 調整背景底色（目前是 `#0B0B0F`）與 accent 色（目前是 `#00D4AA`）。

請保留原本的勾選動畫效果，只修改我指定的部分，然後把完整的修改後程式碼給我。
