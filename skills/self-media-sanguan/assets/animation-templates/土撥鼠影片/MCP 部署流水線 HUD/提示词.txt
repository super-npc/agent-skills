我有一個用 Remotion 寫的場景元件（檔案：Scene12-McpPipeline.tsx），請幫我做以下調整：

1. 修改 `DEFAULT-STEPS` 陣列（目前是 ["Commit", "Push", "PR", "Deploy", "Ready"]）為你自己的流程步驟名稱。
2. 調整各步驟完成的時間點（`STEPS.COMMIT-DONE` 等常數，單位：幀）。
3. 更換底部右側的 Evidence Label 文字（目前是 "GitHub PR" 與 "Vercel Deploy"）。
4. 修改 HUD 的顏色方案（完成色 `#00D4AA`、進行中色 `#4DA3FF`）。

請保留原本的 HUD 滑入動畫與步驟切換效果，只修改我指定的部分，然後把完整的修改後程式碼給我。
