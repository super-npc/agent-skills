我有一個用 Remotion 寫的場景元件（檔案：DevFlowStepsScene.tsx），請幫我做以下調整：

1. 修改四個步驟的標籤文字與顏色（STEPS 陣列中的 label、icon、color）
2. 調整步驟出現的間隔幀數（STEP-DELAY，目前為 30 幀）
3. 修改卡片外框的圓角、背景色與邊框顏色
4. 調整 STEP 文字標籤的字體大小（目前為 20px）

請保留 SVG 描邊動畫與圓形環形漸入效果，只修改我指定的部分，然後把完整的修改後程式碼給我。
