我有一個用 Remotion 寫的場景元件（檔案：Scene23-AddVocals.tsx），請幫我做以下調整：

1. 修改樂器軌道的標籤與顏色（伴奏軌道與人聲軌道）。
2. 調整人聲麥克風圖示的樣式（`MicrophoneIcon` 元件）。
3. 更改人聲軌道的波形顏色（目前是 `colors.accent` 藍色）。
4. 修改背景底色（目前是 `colors.background`）。

請保留原本的波形描邊動畫與人聲加入的漸入效果，只修改我指定的部分，然後把完整的修改後程式碼給我。
