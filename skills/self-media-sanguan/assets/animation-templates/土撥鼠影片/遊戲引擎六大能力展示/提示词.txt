我有一個用 Remotion 寫的場景元件（檔案：Scene03-EngineAbilities.tsx），請幫我做以下調整：

1. 修改六張卡片的標籤文字（目前是：會跑、會跳、會打人、按鍵反應、碰撞偵測、畫面更新）
2. 調整卡片背景色與 SVG 圖示的主色（目前使用 #00D4AA、#FFB547、#FF6B6B、#4DA3FF）
3. 更改整體背景漸層（目前是深藍黑色 linear-gradient(135deg, #0A0E14, #131A24)）
4. 調整每張卡片的彈入延遲時間（目前第一排從 frame 28/53/78 開始，第二排從 108/133/158 開始）

請保留原本的動畫效果，只修改我指定的部分，然後把完整的修改後程式碼給我。
