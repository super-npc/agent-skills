我有一個用 Remotion 寫的場景元件（檔案：SvgCardsScene.tsx），請幫我做以下調整：

1. 修改兩張卡片的文字內容（cardData 陣列中的 text 欄位）
2. 調整兩張卡片的漸層顏色（每張卡片的 colors.c1、colors.c2）
3. 修改頂部標題的數字（目前為「2 個答案」）
4. 調整卡片尺寸（目前為 640×280）

請保留 SVG 描邊動畫與光暈脈動效果，只修改我指定的部分，然後把完整的修改後程式碼給我。
