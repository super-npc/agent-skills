我有一個用 Remotion 寫的場景元件（檔案：Scene20-ComplexPopQuestion.tsx），請幫我做以下調整：

1. 修改左側標籤（目前是 "簡單音樂"）與狀態標籤（目前是 "可以改編"）。
2. 修改右側標題（目前是 "複雜流行音樂"）與疑問標籤（目前是 "是否可行？"）。
3. 調整中間過渡文字（目前是 "那..."）。
4. 更換問號顏色（目前是 `#FFB547` 警告橘）與複雜波形的層數顏色。

請保留原本的左右滑入動畫與問號脈衝效果，只修改我指定的部分，然後把完整的修改後程式碼給我。
