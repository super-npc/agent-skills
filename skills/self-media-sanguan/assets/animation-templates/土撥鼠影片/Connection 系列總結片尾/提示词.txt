我有一個用 Remotion 寫的場景元件（檔案：Scene16-RecapOutro.tsx），請幫我做以下調整：

1. 修改 `recapCards` 陣列中三張卡片的 `title`、`subtitle` 與 `color`，換成你自己的成果摘要。
2. 更換 `channelPills` 陣列（目前是 ["Discord", "Slack", "Email"]）為你的社群頻道名稱。
3. 調整片尾頻道名稱（目前是 "土撥鼠日誌"）。
4. 修改 GitHub 連結文字（目前是 "github.com/woodlandcreature/..."）。

請保留原本的卡片入場動畫與 CTA 按鈕效果，只修改我指定的部分，然後把完整的修改後程式碼給我。
