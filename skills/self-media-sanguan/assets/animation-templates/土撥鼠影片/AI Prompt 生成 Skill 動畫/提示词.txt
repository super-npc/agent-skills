我有一個用 Remotion 寫的場景元件（檔案：Scene11-PromptSkill.tsx），請幫我做以下調整：

1. 修改 `WORD-GROUPS` 陣列中的打字文字內容，換成你自己想要展示的 AI Prompt。
2. 調整三個行動圖示（`ACTION-ITEMS`）的 label 文字與出現時間點（`appearFrame`）。
3. 更換終端機標題（目前是 "Claude Code"）。
4. 修改 accent 顏色（目前是 `#4DA3FF`）與高亮文字顏色（例如 "sonic pi" 的紅色 `#FF6B6B`）。

請保留原本的打字機效果與行動圖示連線動畫，只修改我指定的部分，然後把完整的修改後程式碼給我。
