我有一個用 Remotion 寫的水平長條競賽圖元件（檔案：HorizontalBarRace.tsx），請幫我做以下調整：

1. 把各條目的名稱、數值與顏色改成「___」（修改頂部 ITEMS 陣列，名稱放 name、數值放 value、顏色放 color）
2. 確保陣列仍按 value 由大到小排序，最大值放首位
3. 把長條最大佔寬比例從 70% 改成「___」（修改 BAR-MAX-WIDTH-PERCENT 常數）
4. 調整各條啟動延遲：目前 startFrame = index * 10，想更緊湊請改小；想更分散改大
5. 把右下角單位標注「單位：人」改成「___」
6. 把標題文字「各地區用戶數排行」改成「___」

請保留長條動態增長與數值即時計數邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
