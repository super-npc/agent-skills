我有一個用 Remotion 寫的桑基圖元件（檔案：SankeyDiagram.tsx），請幫我做以下調整：

1. 把 SOURCES 陣列換成我的來源節點（最多 5 個），每項包含 id（名稱）、value（數量）、color（十六進位色碼）、y（垂直位置 0–1）
2. 把 CATEGORIES 陣列換成我的目標節點（最多 4 個），同樣包含 id、value、color、y
3. 把 FLOWS 陣列換成對應的流量關係，每項包含 from（來源 id）、to（目標 id）、value（流量值）
4. 把標題「流量來源分析」改成「___」，副標題改成「___」
5. 把左欄標籤「流量來源」改成「___」，右欄標籤「落地頁面」改成「___」
6. 把路徑淡入的起始影格從 50 調整成 ___（數字越小路徑越早出現）

請保留原本的動畫效果，只修改我指定的部分，然後把完整的修改後程式碼給我。
