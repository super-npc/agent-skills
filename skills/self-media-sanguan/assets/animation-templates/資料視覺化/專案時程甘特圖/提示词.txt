我有一個用 Remotion 寫的專案時程甘特圖元件（檔案：GanttChart.tsx），請幫我做以下調整：

1. 把標題從「專案時程甘特圖」改成「___」
2. 把副標題從「14 週開發週期總覽」改成「___」
3. 在 TASKS 陣列中，把任務名稱（`name`）依序改成「___、___、…」
4. 把各任務的開始週（`start`）與結束週（`end`）依序調整為「___」（單位為週，從 0 起算）
5. 把 `TODAY-WEEK` 從 9 改成 ___（代表今日標記線出現在第幾週）
6. 把每條 bar 的出場間隔從 15 幀改成 ___（數字越小出場越密集）

請保留原本的彈簧展開動畫、發光效果與今日線淡入，只修改我指定的部分，然後把完整的修改後程式碼給我。
