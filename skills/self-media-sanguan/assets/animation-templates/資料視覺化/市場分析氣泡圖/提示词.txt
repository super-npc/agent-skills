我有一個用 Remotion 寫的市場分析氣泡圖元件（檔案：BubbleChart.tsx），請幫我做以下調整：

1. 把標題從「市場分析氣泡圖」改成「___」
2. 把副標題從「市場規模 × 成長率 × 市佔率」改成「___」
3. 在 BUBBLES 陣列中，把 X 軸標籤（`label`）依序改成「___、___、…」
4. 把每個氣泡的 `size` 數值依序改成「___、___、…」（數字越大圓圈越大，建議範圍 10–80）
5. 把三個群組的顏色 `color` 依序改成「___、___、___」（支援任何 hex 色碼）
6. 把每顆氣泡的出場間隔從 8 幀改成 ___（數字越小出場越密集）

請保留原本的彈簧縮放動畫與發光效果，只修改我指定的部分，然後把完整的修改後程式碼給我。
