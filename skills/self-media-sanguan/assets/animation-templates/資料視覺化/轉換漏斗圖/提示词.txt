我有一個用 Remotion 寫的轉換漏斗元件（檔案：Funnel.tsx），請幫我做以下調整：

1. 把漏斗階段數據替換成我的資料（修改頂部 STAGES 陣列，每筆包含 label、value、color）
2. 把標題從「轉換漏斗分析」改成「___」（修改 JSX 中標題文字）
3. 把副標題從「電商全流程轉換率概覽」改成「___」
4. 調整每個階段的最大寬度：把 MAX-WIDTH 從 900 改成「___」（數值越大梯形越寬）
5. 調整各階段彈入速度：把 stiffness 從 110 改成「___」（越大彈入越快）
6. 把右側轉換率標示位置微調：修改 left: centerX + MAX-WIDTH / 2 + 20 中的偏移值

請保留原本的動畫效果，只修改我指定的部分，然後把完整的修改後程式碼給我。
