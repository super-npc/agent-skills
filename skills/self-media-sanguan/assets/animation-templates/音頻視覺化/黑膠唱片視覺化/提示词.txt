我有一個用 Remotion 寫的黑膠唱片視覺化元件（檔案：AudioVinylRecord.tsx），請幫我做以下調整：

1. 把唱片旋轉速度從每幀 1.2 度改成「___」度
2. 把標籤顏色從紅色（#cc3300）改成「___」
3. 把右側頻譜棒數量從 24 根改成「___」根
4. 把標題文字從「黑膠唱片」改成「___」

請保留原本的旋轉動畫與唱針臂，只修改我指定的部分，然後把完整的修改後程式碼給我。
