我有一個用 Remotion 寫的標註框下三分之一元件（檔案：LowerThirdCallout.tsx），請幫我做以下調整：

1. 把標題文字從 "Product Feature" 改成「___」
2. 把副標題文字從 "Tap to learn more" 改成「___」
3. 把標註線和文字框的顏色從琥珀色 #f59e0b 改成「___」
4. 把文字框內的標題文字顏色從深色 #0f172a 改成「___」
5. 把被標註點座標從 (DOT-X=700, DOT-Y=310) 改成 (___,___) 來指向畫面中的不同位置（1920×1080 座標系）
6. 把文字框座標從 (BOX-X=860, BOX-Y=820) 改成 (___,___) 來調整下三分之一的位置（Y 建議在 750-900 之間）

請保留原本的圓點 → 連接線延伸 → 文字框彈出效果，只修改我指定的部分，然後把完整的修改後程式碼給我。
