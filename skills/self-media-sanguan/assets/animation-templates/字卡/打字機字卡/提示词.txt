我有一個用 Remotion 寫的打字機字卡元件（檔案：CardTypewriter.tsx），請幫我做以下調整：

1. 把姓名從 "Jane Smith" 改成「___」（修改頂部 NAME 常數）
2. 把頭銜從 "Creative Director" 改成「___」（修改頂部 TITLE 常數）
3. 把背景色塊從青藍 #0e7490 改成「___」
4. 把頭銜文字顏色從淡青 #a5f3fc 改成「___」
5. 調整打字速度：目前每 2 幀打一個字，把 `/ 2` 改成 `/ 1` 可加速，改成 `/ 3` 可減速
6. 調整姓名與頭銜之間的停頓：目前停頓 20 幀（typeStart + NAME.length * 2 + 20），可改大或改小

請保留打字機效果與閃爍游標，只修改我指定的部分，然後把完整的修改後程式碼給我。
