我有一個用 Remotion 寫的爆炸式倒數計時器元件（檔案：CountdownBlast.tsx），請幫我做以下調整：

1. 把倒數序列從「5-4-3-2-1-GO!」改成「___」（修改 PHASES 陣列）
2. 把每個階段的主色陣列從現有顏色改成「___」（修改 PHASE-COLORS 陣列，需與 PHASES 長度相同）
3. 把能量射線數量從 12 改成「___」（修改 RAY-COUNT 常數）
4. 把每個數字停留幀數從 28 改成「___」（修改 PHASE-FRAMES 常數）
5. 數字衝擊彈出力度調整：stiffness 從 400 改大更衝，改小更柔

請保留能量射線、衝擊波環與 GO! 放射線展開邏輯，只修改我指定的部分，然後把完整的修改後程式碼給我。
